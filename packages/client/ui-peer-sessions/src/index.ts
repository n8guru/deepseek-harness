/** Host loader entry for the peer-sessions poll browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior; this plugin is Client-only. */
export function apply(): void {}
